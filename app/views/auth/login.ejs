<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <style>
        body { font-family: Arial; background: linear-gradient(135deg, #f5e6e6, #e6f2f5); min-height: 100vh; display: flex; align-items: center; justify-content: center; margin: 0; padding: 1rem; }
        .auth-card { background: white; padding: 3rem; border-radius: 20px; box-shadow: 0 20px 60px rgba(0,0,0,0.1); max-width: 400px; width: 100%; }
        h1 { color: #d4a5a5; text-align: center; margin-bottom: 2rem; }
        .error { background: #fee; color: #c33; padding: 1rem; margin-bottom: 1rem; border-radius: 5px; }
        input { width: 100%; padding: 1rem; margin-bottom: 1rem; border: 2px solid #f0f0f0; border-radius: 10px; font-size: 1rem; box-sizing: border-box; }
        button { width: 100%; padding: 1rem; background: linear-gradient(135deg, #d4a5a5, #a5c9d4); color: white; border: none; border-radius: 10px; font-weight: 600; cursor: pointer; }
        .links { text-align: center; margin-top: 1rem; }
        .links a { color: #a5c9d4; text-decoration: none; }
        .demo-section { margin-top: 2rem; padding: 1.5rem; background: rgba(212, 165, 165, 0.1); border-radius: 15px; }
        .demo-title { text-align: center; font-weight: 600; color: #666; margin-bottom: 1rem; }
        .demo-accounts { display: grid; grid-template-columns: 1fr 1fr; gap: 0.5rem; }
        .demo-btn { padding: 0.5rem; background: white; border: 1px solid #ddd; border-radius: 8px; font-size: 0.8rem; cursor: pointer; text-align: center; transition: all 0.2s; }
        .demo-btn:hover { background: #f0f0f0; border-color: #a5c9d4; }
        @media (max-width: 480px) { .demo-accounts { grid-template-columns: 1fr; } }
    </style>
</head>
<body>
    <div class="auth-card">
        <h1>Connexion ADSIAM</h1>
        
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        
        <form method="POST" action="/connexion">
            <input type="email" name="email" value="<%= email %>" placeholder="Email" required>
            <input type="password" name="password" placeholder="Mot de passe" required>
            <button type="submit">Se connecter</button>
        </form>
        
        <div class="links">
            <a href="/inscription">Créer un compte</a><br>
            <a href="/formations/catalogue">Voir le catalogue sans connexion</a>
        </div>
        
        <!-- Section comptes de démonstration -->
        <div class="demo-section">
            <div class="demo-title">Comptes de démonstration</div>
            <div class="demo-accounts">
                <button class="demo-btn" onclick="fillDemo('sophie.martin@example.com', 'StudentPass123!')">
                    Étudiant
                </button>
                <button class="demo-btn" onclick="fillDemo('admin@formapro.fr', 'AdminPassword123!')">
                    Admin
                </button>
            </div>
        </div>
    </div>
    
    <script>
        function fillDemo(email, password) {
            document.querySelector('[name="email"]').value = email;
            document.querySelector('[name="password"]').value = password;
        }
    </script>
</body>
</html>