<!-- app/views/formations/detail.ejs -->
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-pink: #d4a5a5;
            --primary-blue: #a5c9d4;
            --light-pink: #f5e6e6;
            --light-blue: #e6f2f5;
            --soft-gray: #f8f9fa;
            --text-dark: #4a4a4a;
            --text-light: #8a8a8a;
            --white: #ffffff;
            --success: #28a745;
            --gradient-pink: linear-gradient(135deg, #f5e6e6, #d4a5a5);
            --gradient-blue: linear-gradient(135deg, #e6f2f5, #a5c9d4);
            --gradient-contact: linear-gradient(135deg, #d4a5a5, #a5c9d4);
            --gradient-purple: linear-gradient(135deg, #e8d5ff, #c7a3ff);
            --gradient-green: linear-gradient(135deg, #d4f5e6, #a5d4b5);
            --gradient-orange: linear-gradient(135deg, #ffe6d4, #ffb3a5);
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            line-height: 1.6;
            color: var(--text-dark);
            background: var(--soft-gray);
        }

        /* Header */
        header {
            background: var(--white);
            box-shadow: 0 2px 20px rgba(0,0,0,0.08);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        nav {
            max-width: 1400px;
            margin: 0 auto;
            padding: 1.5rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.8rem;
            text-decoration: none;
        }

        .logo-icon {
            width: 50px;
            height: 50px;
            background: var(--gradient-contact);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 1.3rem;
        }

        .logo-text {
            font-weight: 700;
            font-size: 1.8rem;
        }

        .forma {
            color: var(--primary-pink);
        }

        .pro {
            color: var(--primary-blue);
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-links a {
            color: var(--text-dark);
            text-decoration: none;
            font-weight: 500;
            padding: 0.8rem 1.5rem;
            border-radius: 25px;
            transition: all 0.3s;
        }

        .nav-links a:hover {
            background: var(--light-blue);
            color: var(--primary-blue);
        }

        /* Breadcrumb */
        .breadcrumb {
            background: var(--white);
            padding: 1rem 0;
            border-bottom: 1px solid var(--soft-gray);
        }

        .breadcrumb-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .breadcrumb-nav {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.9rem;
        }

        .breadcrumb-nav a {
            color: var(--text-light);
            text-decoration: none;
            transition: color 0.3s;
        }

        .breadcrumb-nav a:hover {
            color: var(--primary-blue);
        }

        /* Formation Hero */
        .formation-hero {
            background: var(--gradient-contact);
            padding: 4rem 0;
            color: white;
            position: relative;
            overflow: hidden;
        }

        .formation-hero::before {
            content: '';
            position: absolute;
            top: -30%;
            right: -20%;
            width: 40%;
            height: 160%;
            background: var(--gradient-blue);
            border-radius: 50%;
            opacity: 0.3;
            transform: rotate(-15deg);
        }

        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            position: relative;
            z-index: 2;
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 3rem;
            align-items: center;
        }

        .hero-main {
            text-align: left;
        }

        .formation-badge {
            background: rgba(255, 255, 255, 0.9);
            color: var(--primary-pink);
            padding: 0.5rem 1.5rem;
            border-radius: 50px;
            display: inline-block;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
            font-weight: 600;
        }

        .formation-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            display: block;
        }

        .formation-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .formation-subtitle {
            font-size: 1.2rem;
            opacity: 0.9;
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .hero-sidebar {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 2rem;
            backdrop-filter: blur(10px);
            text-align: center;
        }

        .price-display {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 1rem;
        }

        .btn {
            width: 100%;
            padding: 1.2rem;
            border: none;
            border-radius: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            font-size: 1.1rem;
            margin-bottom: 1rem;
        }

        .btn-primary {
            background: white;
            color: var(--primary-pink);
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.3);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.3);
        }

        .hero-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1rem;
            margin-top: 2rem;
        }

        .stat-item {
            text-align: center;
            padding: 1rem;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
        }

        .stat-value {
            font-size: 1.5rem;
            font-weight: 700;
            display: block;
        }

        .stat-label {
            font-size: 0.9rem;
            opacity: 0.8;
        }

        /* Main Content */
        .main-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 4rem 2rem;
        }

        .content-section {
            background: var(--white);
            border-radius: 25px;
            padding: 3rem;
            margin-bottom: 3rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
        }

        .section-title {
            font-size: 2rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .section-icon {
            width: 50px;
            height: 50px;
            background: var(--gradient-blue);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.3rem;
        }

        /* Bloc Structure */
        .bloc-container {
            margin-bottom: 2rem;
        }

        .bloc-header {
            background: var(--gradient-pink);
            color: white;
            padding: 1.5rem 2rem;
            border-radius: 15px 15px 0 0;
            display: flex;
            align-items: center;
            gap: 1rem;
            cursor: pointer;
            transition: all 0.3s;
            user-select: none;
        }

        .bloc-header:hover {
            background: var(--gradient-blue);
        }

        .bloc-header.expanded {
            border-radius: 15px 15px 0 0;
        }

        .bloc-header.collapsed {
            border-radius: 15px;
        }

        .bloc-number {
            background: rgba(255, 255, 255, 0.2);
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 1.2rem;
        }

        .bloc-title {
            flex: 1;
            font-size: 1.3rem;
            font-weight: 600;
        }

        .bloc-toggle {
            font-size: 1.2rem;
            transition: transform 0.3s;
        }

        .bloc-header.expanded .bloc-toggle {
            transform: rotate(180deg);
        }

        .bloc-content {
            background: white;
            border: 2px solid var(--soft-gray);
            border-top: none;
            border-radius: 0 0 15px 15px;
            overflow: hidden;
            transition: all 0.3s;
        }

        .bloc-content.collapsed {
            max-height: 0;
            border: none;
        }

        .bloc-content.expanded {
            max-height: 1000px;
        }

        .modules-list {
            padding: 0;
        }

        .module-item {
            border-bottom: 1px solid var(--soft-gray);
            padding: 1.5rem 2rem;
            transition: background 0.3s;
        }

        .module-item:last-child {
            border-bottom: none;
        }

        .module-item:hover {
            background: var(--light-blue);
        }

        .module-header {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .module-icon {
            width: 40px;
            height: 40px;
            background: var(--gradient-blue);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
            flex-shrink: 0;
        }

        .module-info {
            flex: 1;
        }

        .module-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.3rem;
        }

        .module-duration {
            font-size: 0.9rem;
            color: var(--text-light);
        }

        .module-content {
            margin-left: 3rem;
            color: var(--text-light);
            line-height: 1.6;
        }

        .module-topics {
            list-style: none;
            margin-top: 0.8rem;
        }

        .module-topics li {
            padding: 0.3rem 0;
            position: relative;
            padding-left: 1.5rem;
        }

        .module-topics li::before {
            content: '•';
            color: var(--primary-blue);
            font-weight: 600;
            position: absolute;
            left: 0;
        }

        /* Objectifs section */
        .objectifs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .objectif-card {
            background: var(--light-blue);
            padding: 2rem;
            border-radius: 15px;
            text-align: center;
        }

        .objectif-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            display: block;
        }

        .objectif-title {
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 1rem;
        }

        .objectif-description {
            color: var(--text-light);
            line-height: 1.6;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-content {
                grid-template-columns: 1fr;
                gap: 2rem;
                text-align: center;
            }

            .hero-stats {
                grid-template-columns: 1fr;
            }

            .formation-title {
                font-size: 2rem;
            }

            .objectifs-grid {
                grid-template-columns: 1fr;
            }

            .module-content {
                margin-left: 0;
                margin-top: 1rem;
            }
        }

        /* Animations */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <nav>
            <a href="/" class="logo">
                <div class="logo-icon">P+</div>
                <div class="logo-text">
                    <span class="forma">FORMA</span><span class="pro">PRO+</span>
                </div>
            </a>
            <ul class="nav-links">
                <li><a href="/formations">Formations</a></li>
                <% if (typeof user !== 'undefined' && user) { %>
                    <li><a href="/dashboard">Mon compte</a></li>
                    <li><a href="/auth/logout">Déconnexion</a></li>
                <% } else { %>
                    <li><a href="/connexion">Connexion</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <!-- Breadcrumb -->
    <div class="breadcrumb">
        <div class="breadcrumb-container">
            <nav class="breadcrumb-nav">
                <a href="/">Accueil</a>
                <span>›</span>
                <a href="/formations/catalogue">Catalogue</a>
                <span>›</span>
                <span><%= formation.titre %></span>
            </nav>
        </div>
    </div>

    <!-- Formation Hero -->
    <section class="formation-hero">
        <div class="hero-content">
            <div class="hero-main">
                <div class="formation-badge"><%= formation.badge %></div>
                <div class="formation-icon"><%= formation.icon %></div>
                <h1 class="formation-title"><%= formation.titre %></h1>
                <p class="formation-subtitle"><%= formation.description %></p>
            </div>
            
            <div class="hero-sidebar">
                <div class="price-display">
                    <%= formation.prix === 0 ? 'Gratuit' : formation.prix + '€' %>
                </div>
                
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/dashboard/formation/<%= formation.id %>" class="btn btn-primary">
                        Commencer la formation
                    </a>
                    <button class="btn btn-secondary">
                        Ajouter aux favoris
                    </button>
                <% } else { %>
                    <button class="btn btn-primary" onclick="redirectToLogin()">
                        Commencer la formation
                    </button>
                    <button class="btn btn-secondary" onclick="showLoginPrompt()">
                        Ajouter aux favoris
                    </button>
                <% } %>

                <div class="hero-stats">
                    <div class="stat-item">
                        <span class="stat-value"><%= formation.modules_count %></span>
                        <span class="stat-label">Modules</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value"><%= formation.duree %></span>
                        <span class="stat-label">Durée</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value"><%= formation.niveau %></span>
                        <span class="stat-label">Niveau</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Objectifs -->
        <section class="content-section fade-in">
            <h2 class="section-title">
                <div class="section-icon">🎯</div>
                Objectifs de la formation
            </h2>
            <div class="objectifs-grid">
                <div class="objectif-card">
                    <div class="objectif-icon">💬</div>
                    <h3 class="objectif-title">Maîtriser la communication</h3>
                    <p class="objectif-description">Développer vos compétences relationnelles avec les résidents, familles et équipes</p>
                </div>
                <div class="objectif-card">
                    <div class="objectif-icon">🛡️</div>
                    <h3 class="objectif-title">Assurer la sécurité</h3>
                    <p class="objectif-description">Appliquer les protocoles d'hygiène et prévenir les risques domestiques</p>
                </div>
                <div class="objectif-card">
                    <div class="objectif-icon">🤲</div>
                    <h3 class="objectif-title">Accompagner avec bienveillance</h3>
                    <p class="objectif-description">Respecter la dignité et favoriser l'autonomie des personnes accompagnées</p>
                </div>
            </div>
        </section>

        <!-- Programme détaillé -->
        <section class="content-section fade-in">
            <h2 class="section-title">
                <div class="section-icon">📚</div>
                Programme détaillé - <%= formation.titre %>
            </h2>

            <% 
            // Définir les modules selon le domaine de la formation
            let modules = [];
            
            if (formation.domaine === 'communication') {
                modules = [
                    {
                        numero: 1,
                        titre: "Bases de la communication professionnelle",
                        duree: "45 min",
                        sujets: ["Principes de la communication", "Écoute active", "Communication non-verbale", "Adaptation au public"]
                    },
                    {
                        numero: 2,
                        titre: "Relations avec les familles",
                        duree: "50 min",
                        sujets: ["Accueil des familles", "Gestion des demandes", "Transmission d'informations", "Situations délicates"]
                    },
                    {
                        numero: 3,
                        titre: "Travail en équipe",
                        duree: "40 min",
                        sujets: ["Communication interprofessionnelle", "Transmissions écrites", "Réunions d'équipe", "Gestion des conflits"]
                    },
                    {
                        numero: 4,
                        titre: "Gestion des émotions",
                        duree: "35 min",
                        sujets: ["Comprendre les émotions", "Techniques d'apaisement", "Gestion de l'agressivité", "Soutien psychologique"]
                    },
                    {
                        numero: 5,
                        titre: "Communication non-verbale avancée",
                        duree: "30 min",
                        sujets: ["Langage corporel", "Proxémie", "Mimiques faciales", "Adaptation aux troubles"]
                    }
                ];
            } else if (formation.domaine === 'hygiene') {
                modules = [
                    {
                        numero: 6,
                        titre: "Hygiène personnelle et professionnelle",
                        duree: "60 min",
                        sujets: ["Lavage des mains, gants, surblouses, masques", "Tenue adaptée", "Prévention des contaminations croisées"]
                    },
                    {
                        numero: 7,
                        titre: "Sécurité et produits ménagers",
                        duree: "55 min",
                        sujets: ["Lecture des pictogrammes de danger", "Mélanges dangereux à éviter", "Stockage sécurisé"]
                    },
                    {
                        numero: 8,
                        titre: "Prévention des risques domestiques",
                        duree: "50 min",
                        sujets: ["Chutes, brûlures, électrocution", "Sécurisation des lieux", "Détection de situations dangereuses"]
                    },
                    {
                        numero: 9,
                        titre: "Prévention des infections et protocoles",
                        duree: "65 min",
                        sujets: ["Désinfection des surfaces", "Gestion des objets contaminés", "Prévention des infections nosocomiales"]
                    }
                ];
            } else if (formation.domaine === 'ergonomie') {
                modules = [
                    {
                        numero: 10,
                        titre: "Bonnes postures et manutention",
                        duree: "70 min",
                        sujets: ["Techniques de portage", "Postures de travail", "Prévention des TMS", "Échauffements"]
                    },
                    {
                        numero: 11,
                        titre: "Transferts et déplacements sécurisés",
                        duree: "65 min",
                        sujets: ["Techniques de transfert", "Utilisation des aides techniques", "Accompagnement des déplacements"]
                    },
                    {
                        numero: 12,
                        titre: "Utilisation du matériel médical",
                        duree: "55 min",
                        sujets: ["Lits médicalisés", "Fauteuils roulants", "Matériel de marche", "Maintenance et nettoyage"]
                    }
                ];
            } else if (formation.domaine === 'urgences') {
                modules = [
                    {
                        numero: 13,
                        titre: "Reconnaître et évaluer les urgences",
                        duree: "60 min",
                        sujets: ["Signes d'alerte", "Évaluation de l'état général", "Prise de constantes", "Quand alerter"]
                    },
                    {
                        numero: 14,
                        titre: "Gestes de premiers secours",
                        duree: "75 min",
                        sujets: ["Position latérale de sécurité", "Massage cardiaque", "Utilisation du défibrillateur", "Gestion des blessures"]
                    },
                    {
                        numero: 15,
                        titre: "Conduite à tenir en cas d'urgence",
                        duree: "50 min",
                        sujets: ["Procédures d'alerte", "Gestion du stress", "Accompagnement jusqu'aux secours"]
                    },
                    {
                        numero: 16,
                        titre: "Situations spécifiques",
                        duree: "45 min",
                        sujets: ["Malaises cardiaques", "AVC", "Chutes graves", "Intoxications"]
                    },
                    {
                        numero: 17,
                        titre: "Pratique et évaluation",
                        duree: "60 min",
                        sujets: ["Mises en situation", "Évaluation pratique", "Révisions", "Certification"]
                    }
                ];
            } else {
                // Modules par défaut
                modules = [
                    {
                        numero: 1,
                        titre: "Introduction à la formation",
                        duree: "30 min",
                        sujets: ["Objectifs", "Méthode", "Ressources", "Évaluation"]
                    },
                    {
                        numero: 2,
                        titre: "Fondamentaux théoriques",
                        duree: "45 min",
                        sujets: ["Concepts de base", "Réglementation", "Bonnes pratiques", "Cas pratiques"]
                    },
                    {
                        numero: 3,
                        titre: "Mise en pratique",
                        duree: "50 min",
                        sujets: ["Exercices", "Études de cas", "Simulations", "Retours d'expérience"]
                    }
                ];
            }
            %>

            <div class="modules-list">
                <% modules.forEach((module, index) => { %>
                    <div class="module-item">
                        <div class="module-header">
                            <div class="module-icon"><%= module.numero %></div>
                            <div class="module-info">
                                <div class="module-title">Module <%= module.numero %> : <%= module.titre %></div>
                                <div class="module-duration">Durée : <%= module.duree %></div>
                            </div>
                        </div>
                        <div class="module-content">
                            <p>Contenu abordé :</p>
                            <ul class="module-topics">
                                <% module.sujets.forEach(sujet => { %>
                                    <li><%= sujet %></li>
                                <% }); %>
                            </ul>
                        </div>
                    </div>
                <% }); %>
            </div>
        </section>

        <!-- Pédagogie -->
        <section class="content-section fade-in">
            <h2 class="section-title">
                <div class="section-icon">🎓</div>
                Méthode pédagogique
            </h2>
            <div class="objectifs-grid">
                <div class="objectif-card">
                    <div class="objectif-icon">🎬</div>
                    <h3 class="objectif-title">Vidéos explicatives</h3>
                    <p class="objectif-description">Démonstrations pratiques et mises en situation réelles</p>
                </div>
                <div class="objectif-card">
                    <div class="objectif-icon">📝</div>
                    <h3 class="objectif-title">Quiz interactifs</h3>
                    <p class="objectif-description">Évaluation des connaissances après chaque module</p>
                </div>
                <div class="objectif-card">
                    <div class="objectif-icon">📄</div>
                    <h3 class="objectif-title">Ressources PDF</h3>
                    <p class="objectif-description">Fiches pratiques et guides de référence téléchargeables</p>
                </div>
            </div>
        </section>
    </div>

    <script>
        function toggleBloc(header) {
            const isExpanded = header.classList.contains('expanded');
            const content = header.nextElementSibling;
            
            if (isExpanded) {
                header.classList.remove('expanded');
                header.classList.add('collapsed');
                content.classList.remove('expanded');
                content.classList.add('collapsed');
            } else {
                header.classList.remove('collapsed');
                header.classList.add('expanded');
                content.classList.remove('collapsed');
                content.classList.add('expanded');
            }
        }

        function redirectToLogin() {
            sessionStorage.setItem('redirectAfterLogin', window.location.pathname);
            window.location.href = '/connexion';
        }

        function showLoginPrompt() {
            alert('Connectez-vous pour ajouter cette formation à vos favoris !');
            redirectToLogin();
        }

        // Animation au scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.animation = 'fadeInUp 0.8s ease forwards';
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => {
            observer.observe(el);
        });

        // Ouvrir le premier bloc par défaut
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Formation détail chargée : <%= formation.titre %>');
        });
    </script>
</body>
</html>