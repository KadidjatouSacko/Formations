<%- include('../layout/main', { body: `
    <!-- Dashboard Header -->
    <div class="dashboard-header">
        <div class="welcome-content">
            <div class="welcome-text">
                <h1>Bonjour ${user.prenom} !</h1>
                <p>Prêt à continuer votre parcours de formation ? Voici un aperçu de votre progression.</p>
            </div>
            <div class="user-stats">
                <div class="stat-card">
                    <div class="stat-icon">📊</div>
                    <div class="stat-value">${progression.pourcentage}%</div>
                    <div class="stat-label">Progression</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">⏱️</div>
                    <div class="stat-value">${progression.tempsTotal}</div>
                    <div class="stat-label">Temps passé</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">🏆</div>
                    <div class="stat-value">${progression.certificatsObtenus}</div>
                    <div class="stat-label">Certificats</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Progression Globale -->
    <section class="progress-section">
        <div class="section-header">
            <h2 class="section-title">📈 Votre progression</h2>
        </div>
        
        <div class="overall-progress">
            <div class="progress-circle">
                <div class="progress-percentage">${progression.pourcentage}%</div>
            </div>
            <p class="progress-text">Excellent travail ! Continuez sur cette lancée.</p>
            <div class="progress-details">
                <div class="progress-detail">
                    <div class="detail-value">${progression.modulesTermines}/${progression.modulesTotal}</div>
                    <div class="detail-label">Modules</div>
                </div>
                <div class="progress-detail">
                    <div class="detail-value">${progression.tempsTotal}</div>
                    <div class="detail-label">Temps</div>
                </div>
                <div class="progress-detail">
                    <div class="detail-value">${progression.scoreeMoyen}%</div>
                    <div class="detail-label">Score moyen</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Formations en cours -->
    <section class="formations-section">
        <div class="section-header">
            <h2 class="section-title">📚 Formations en cours</h2>
        </div>
        
        <div class="formations-grid">
            <% formations.forEach(formation => { %>
                <div class="formation-card" data-formation-id="${formation.id}">
                    <div class="formation-header">
                        <div class="formation-icon">${formation.icon}</div>
                        <div class="formation-status status-${formation.status}">
                            <%= formation.status === 'active' ? 'En cours' : 'En pause' %>
                        </div>
                    </div>
                    
                    <h3 class="formation-title">${formation.title}</h3>
                    
                    <div class="formation-progress">
                        <div class="progress-label">
                            <span>Progression</span>
                            <span>${formation.progression}%</span>
                        </div>
                        <div class="progress-bar">
                            <div class="progress-fill" style="width: ${formation.progression}%"></div>
                        </div>
                    </div>
                    
                    <div class="formation-meta">
                        <div class="meta-item">
                            <span>⏱️ ${formation.tempsEcoule}</span>
                        </div>
                        <div class="meta-item">
                            <span>📖 ${formation.prochainModule}</span>
                        </div>
                    </div>
                    
                    <div class="formation-actions">
                        <a href="/formation/lecteur/${formation.id}" class="btn btn-primary">
                            <%= formation.status === 'active' ? 'Continuer' : 'Reprendre' %>
                        </a>
                        <button class="btn btn-outline btn-progression" data-formation-id="${formation.id}">
                            📊 Progression
                        </button>
                    </div>
                </div>
            <% }); %>
        </div>
    </section>

    <!-- Actions rapides -->
    <section class="actions-section">
        <h2 class="section-title">⚡ Actions rapides</h2>
        <div class="actions-grid">
            <div class="action-card" onclick="window.location.href='/entreprise/formations'">
                <div class="action-icon">🔍</div>
                <div class="action-title">Découvrir de nouvelles formations</div>
                <div class="action-description">Explorez notre catalogue</div>
            </div>
            <div class="action-card" onclick="window.location.href='/dashboard/etudiant/certificats'">
                <div class="action-icon">📜</div>
                <div class="action-title">Mes certificats</div>
                <div class="action-description">Téléchargez vos attestations</div>
            </div>
            <div class="action-card" onclick="window.location.href='/dashboard/etudiant/statistiques'">
                <div class="action-icon">📈</div>
                <div class="action-title">Statistiques détaillées</div>
                <div class="action-description">Analysez votre progression</div>
            </div>
            <div class="action-card" onclick="window.location.href='/entreprise/contact'">
                <div class="action-icon">💬</div>
                <div class="action-title">Support & aide</div>
                <div class="action-description">Contactez notre équipe</div>
            </div>
        </div>
    </section>

    <!-- Activité récente -->
    <section class="activity-section">
        <div class="recent-activity">
            <div class="section-header">
                <h2 class="section-title">🕐 Activité récente</h2>
                <a href="/dashboard/etudiant/progression" class="btn btn-secondary">Tout voir</a>
            </div>
            <div class="activity-list">
                <% activites.forEach(activite => { %>
                    <div class="activity-item">
                        <div class="activity-icon ${activite.type}">${activite.icon}</div>
                        <div class="activity-content">
                            <div class="activity-title">${activite.title}</div>
                            <div class="activity-description">${activite.description}</div>
                            <div class="activity-time">${formatTime(activite.timestamp)}</div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </section>
` }) %>